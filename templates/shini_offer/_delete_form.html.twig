<form method="post" action="{{ path('shini.offer.delete', {'id': shini_offer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shini_offer.id) }}">
    <button class="btn btn-outline-danger"><i class="fas fa-user-minus  text-center"></i> Delete</button>
</form>
