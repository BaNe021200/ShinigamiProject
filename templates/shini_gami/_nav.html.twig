<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
        <a class="navbar-brand" href="{{ path('shini.index') }}">ShiniGamiLaser</a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                {#<li class="nav-item">
                    <a class="nav-link" href="{{ path('home') }}">Home</a>
                </li>#}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('shini.about')}}">A propos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('shini.offers') }}">Les offres du moment</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('shini.contact') }}">Nous contacter</a>
                </li>
                <li class="nav-item float-right">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="nav-link" href="{{ path('security.logout') }}">
                            Déconnexion
                    </a>
                        {% else %}
                            <a class="nav-link" href="{{ path('shini.signInUp') }}">
                                Connexion
                            </a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>