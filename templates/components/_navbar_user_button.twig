<ul class="nav {#align-items-right mr-0 mr-sm-2#} mr-2 mr-sm-4 mt-2">
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="nav-item dropdown">
            <!-- User button -->
            <button id="userButton" type="button" class="btn btn-success dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                {{ app.user.name ~' '~ app.user.lastName }}
            </button>
    {#        <a class="nav-link dropdown" href="#" id="navbarDropdown-2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                {{ app.actions.name ~' '~ app.actions.lastName }}
            </a>#}

            {# ROLE_PLAYER #}
            {% if is_granted('ROLE_PLAYER') %}
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <h6 class="dropdown-header">Mon profil</h6>
                    <a class="dropdown-item" href="{{ path('shini.player.profile') }}"><i class="fas fas fa-pen">&nbsp;</i> Editer</a>
                    <a class="dropdown-item" href="#{# TODO: {{ path('shini.offer.mine') }}#}"><i class="fas fa-eye">&nbsp;</i> Mes offres</a>
                    <div class="dropdown-divider"></div>
                    <h6 class="dropdown-header">Lister</h6>
                    <a class="dropdown-item" href="{{ path('shini.offer.list') }}"><i class="fas fa-eye">&nbsp;</i> Les offres</a>
                    <a class="dropdown-item" href="{{ path('shini.player.list') }}"><i class="fas fa-eye">&nbsp;</i> Les joueurs</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('secure.signout') }}"><i class="fas fa-sign-out-alt"></i>&nbsp;Se déconnecter</a>
                    <!-- TODO: A completer avec 'Mes jeux', 'Mes équipes', etc... -->
                </div>

            {# ROLE_STAFF and ROLE_ADMIN #}
            {% elseif is_granted('ROLE_STAFF') %}
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <h6 class="dropdown-header">Mon profil</h6>
                    <a class="dropdown-item" href="{{ path('shini.staff.profile') }}"><i class="fas fas fa-pen">&nbsp;</i> Editer</a>
                    <a class="dropdown-item" href="#{# TODO: {{ path('shini.player.staff.mine') }}#}"><i class="fas fa-eye">&nbsp;</i> Mes joueurs</a>
                    <!-- TODO: A completer -->
                    <div class="dropdown-divider"></div>
                    <h6 class="dropdown-header">Créer</h6>
                    <a class="dropdown-item" href="{{ path('shini.offer.new') }}"><i class="fas fa-plus-square">&nbsp;</i> Une offre</a>
                    <a class="dropdown-item" href="{{ path('shini.center.new') }}"><i class="fas fa-plus-square">&nbsp;</i> Un centre</a>
                    <div class="dropdown-divider"></div>
                    <h6 class="dropdown-header">Lister</h6>
                    <a class="dropdown-item" href="{{ path('shini.offer.list') }}"><i class="fas fa-eye">&nbsp;</i> Les offres</a>
                    <a class="dropdown-item" href="{{ path('shini.center.list') }}"><i class="fas fa-eye">&nbsp;</i> Les centres</a>
                    <a class="dropdown-item" href="{{ path('shini.staff.list') }}"><i class="fas fa-eye">&nbsp;</i> Le staff</a>
                    <a class="dropdown-item" href="{{ path('shini.player.list') }}"><i class="fas fa-eye">&nbsp;</i> Les joueurs</a>
                    <a class="dropdown-item" href="{{ path('shini.player.search') }}"><i class="fas fa-search"></i> Rechercher un client</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('secure.signout') }}"><i class="fas fa-sign-out-alt"></i>&nbsp;Se déconnecter</a>
                </div>
            {% endif %}
        </li>
    {% else %}
        {# TODO: mettre les deux formulaire inscription et connection dans le dropdown #}
        <li class="nav-item dropdown">
            <button id="userButton" type="button" class="btn btn-secodary dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
               Connexion/Inscription
            </button>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="{{ path('secure.signinup', {'sign' : 'in'}) }}">Se&nbsp;connecter</a>
                <a class="dropdown-item" href="{{ path('secure.signinup', {'sign' : 'up'}) }}">S'inscrire</a>
            </div>
        </li>
    {% endif %}
</ul>