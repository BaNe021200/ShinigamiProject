<h3>You did it! You registered!</h3>

Hi {{ player.name }}! You're successfully registered.

{# example, assuming you have a route named "login" #}
To login, go to: <a href="{{ url('secure.email.confirmation',{userId:player.id, token: player.account.confirmationToken }) }}">cliquez sur ce lien pour valider votre compte</a>.

Thanks!

{# Makes an absolute URL to the /images/logo.png file #}
{#
<img src="{{ absolute_url(asset('images/logo.png')) }}">#}
