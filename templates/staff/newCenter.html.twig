{% extends 'staff/layout.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Créer un nouveau centre</h1>


        <div class="container">
            {{ form(form) }}
           {# <form action="{{ path('shiniStaff.create.new.center') }}" method="post" >
                <div class="input-group mb-3">
                    <div class="input-group-prepend">
                        <button class="btn btn-info" id="basic-addon1">Créer</button>
                    </div>
                    <input type="text" name="newCenter" class="form-control" placeholder="Creation du centre" aria-label="UserPseudo"
                           aria-describedby="basic-addon1">
                </div>
            </form>#}
        </div>
{% endblock %}

{% block title %}
    new center
{% endblock %}

