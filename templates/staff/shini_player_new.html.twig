{% extends 'base.html.twig' %}

{% block title %}New ShiniPlayer{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/cities.css') }}">
{% endblock %}


{% block body %}
    <h1>Create new ShiniPlayer</h1>

    <div class="container">
        {{ form_start(form, {attr:{novalidate: 'novalidate' }}) }}

        <div class="row">

            <div class="col-md-4">{{ form_row(form.name) }}</div>
            <div class="col-md-4">{{ form_row(form.lastname) }}</div>
            <div class="col-md-4">{{ form_row(form.nickName) }}</div>

        </div>

        <div class="row">

            <div class="col-md-4">{{ form_row(form.birthday) }}</div>
            <div class="col-md-4">{{ form_row(form.email) }}</div>
            <div class="col-md-4">{{ form_row(form.phone) }}</div>
        </div>

        <div class="row">

            <div class="col-md-4">{{ form_row(form.address) }}</div>
            <div class="col-md-4">{{ form_row(form.postalCode,{'id' : 'code'} ) }}<div id="output"></div></div>
            <div class="col-md-4">{{ form_row(form.city,{'id' : 'city'}) }}

            {#<div class="row">

                <section id="input" class="input-group lg-3">

                    <div class="half input-group-prepend">

                        <input id="code" placeholder="Code postal"  name="postal_code"  autocomplete="off" autofocus="" value="">
                    </div>
                    <div class="half">
                        <input id="city" placeholder="Ville" name="city" autocomplete="off" value="">
                    </div>
                </section>
                <section id="output"></section>

            </div>#}

            </div>
        </div>

        <div class="row">
            <div class="col-md-6">{{ form_row(form.password.first) }}</div>
            <div class="col-md-6">{{ form_row(form.password.second) }}</div>

        </div>


        {{ form_widget(form) }}

        {{ form_end(form) }}
        <a href="{{ path('shini.player.list') }}"><button class="btn btn-info ">{{ button_label|default('Retour à la liste') }}</button></a>

    </div>
{% endblock body %}

{% block script %}
   {{ parent() }}

   {# <script src="{{ asset('js/jQuery.js') }}"></script>#}
    <script src="{{ asset('js/cities_api.js') }}"></script>


{% endblock script %}
